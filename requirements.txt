# Pinned dependencies for the whole project.
#
# The SCORED pipeline (run.sh -> generate_features.py -> predict.py) only imports
# the first group. The rest power the Streamlit demo app and the LLM insight
# layer, and are included here so the app deploys on Streamlit Cloud (which
# installs from this file). Versions are pinned so the committed model unpickles.

# --- core forecasting / scored path ---
pandas==2.3.3
numpy==2.2.6
lightgbm==4.6.0
pyarrow==24.0.0

# --- demo app + AI insight layer ---
streamlit==1.58.0
plotly==6.8.0
requests==2.34.2
python-dotenv==1.2.2
